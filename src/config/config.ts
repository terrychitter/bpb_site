import { fonts, palette } from "./theme";
import text from "./text";
import animation from "./animation";
import links from "./links";
import blobGalleryConfig from "./blob_gallery";

const config = {
  theme: {
    fonts: fonts,
    palette: palette,
  },
  text: text,
  animation: animation,
  links: links,
  blobGalleryConfig: blobGalleryConfig,
};

export default config;
